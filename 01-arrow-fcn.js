// JS refresher
const name = "Ellis";
let age = 22;
age = 25;

const summerizeUser = (userName, userAge) => {
  return `name is ${userName} and age is ${userAge}`;
};

console.log(summerizeUser(name, age));

// const add = (a, b) => {
//   return a + b;
// };
// console.log(add(1, 2));

// const add = (a, b) => a + b;
// console.log(add(1, 2));

// const addOne = a => a + 1;
// console.log(addOne(2));

